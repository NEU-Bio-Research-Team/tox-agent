model:
  name: "GATv2MolecularPredictor"
  num_layers: 4
  hidden_dim: 128
  num_heads: 4
  dropout: 0.2
  use_residual: true
  use_jk: true
  jk_mode: "cat"  # cat, max, mean, lstm
  pooling: "set2set"  # set2set, attentive, global_mean, global_max, global_sum

training:
  num_epochs: 120
  batch_size: 64
  learning_rate: 0.0008
  weight_decay: 0.00001
  early_stopping_patience: 25
  early_stopping_metric: "macro_auc_roc"  # macro_auc_roc, macro_pr_auc, macro_f1, loss
  loss_type: "focal"  # focal, weighted_bce, bce
  focal_alpha: 0.25
  focal_gamma: 2.0
  use_task_pos_weights: true
  calibrate_thresholds: true
  threshold_min: 0.05
  threshold_max: 0.95
  threshold_step: 0.01
  fallback_threshold: 0.5

data:
  cache_dir: "./data"
  split_type: "scaffold"
  seed: 42

output:
  model_dir: "./models/tox21_gatv2_model"
  save_best: true
