workspace:
  mode: full
  primary_dataset: tox21
  clintox_enabled: true
  tox21_enabled: true
  threshold_policy: safety_first
  # Optional explicit override. If set, this wins over threshold_policy.
  # clinical_threshold: 0.35
  message: "Full workspace mode with dual-head pretrained binary models enabled."
