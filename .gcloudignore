# Keep Docker build context small, but DO include models/ for runtime inference.
.gcloudignore
.git/
.gitignore
.vscode/
.firebase/
node_modules/
__pycache__/
.venv/
venv/
tox_env/
*.log
*.tmp
frontend/
notebooks/
outputs/
results/
temp_out.json

# Local documents / photos are not needed in the backend image.
documents photos/

# Keep MolRAG retrieval data in image while excluding the rest of test fixtures.
test_data/**
!test_data/
!test_data/reference_panel.csv
!test_data/screening_library.csv

# Keep all model artifacts in build context (full model build requested).
