# Context is toxagent-control/ itself (see deploy/Dockerfile) — keep the
# production image to what toxagent/ actually imports at runtime.
.git/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
tests/
evals/
docs/
*.egg-info/
