# ToxAgent predictor registry index. A model release lives in exactly one
# reviewable manifest under models/; artifact binaries remain outside Git.
schema_version: 1
models_root: ../../../models
includes:
  - models/herg-tox21-chemberta-v1.yaml
  - models/clintox-smilesgnn-v1.yaml
